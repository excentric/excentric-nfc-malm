spring:
  shell:
    history:
      enabled: false
    log:
      enabled: false
      file: ${user.home}/.malm/spring-shell.log

server:
  port: 8063

music-album-label-maker:
  metadata-directory: ${user.home}/.album-metadata
